{
  "name": "sitemap-kb-auto-updater",
  "version": "1.0.0",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/xml2js": "^0.4.14",
    "bun-types": "latest"
  },
  "dependencies": {
    "@types/node-cron": "^3.0.11",
    "node-cron": "^3.0.3",
    "xml2js": "^0.6.2"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "docker-start": "docker compose up --build -d",
    "docker-stop": "docker compose down"
  }
}
